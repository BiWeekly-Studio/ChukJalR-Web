# 첫 방문 프로모션 지급·메이저 리그 적중률

## 현재 배포 상태

- 서버 DB 마이그레이션 `20260911000042` 및 `toss-promotion` Edge Function은 운영 반영 완료.
- 앱인토스 수정본 **20260911-12**: 2026-09-11 11:18 KST 업로드, **검토 요청 제출 완료 / 검토 중**.
- 현재 출시 버전은 **20260910-11**. 승인 후 **20260911-12의 ‘출시하기’**를 실행하고 현재 출시된 버전이 바뀌었는지 확인해야 한다.
- 아직 새 앱의 자동 호출과 메이저 필터가 일반 사용자에게 배포 완료된 것은 아니다. 서버 기능만 배포했다고 전체 출시 완료로 안내하지 않는다.
- 콘솔: https://apps-in-toss.toss.im/workspace/5291/mini-app/chukjalr/app-build
- deploymentId: `01a08e41-4cf3-7d68-9cbf-6702734a2f8d`
- 파일: `artifacts/chukjalr-promotion-major-20260911.ait`
- SHA256: `64af28b3ddd07c53812386d0c530cdc49be232768ac91bdcaf292f68d60bef2e`

## 수정 내용

- 로그인된 앱 방문 시 서버에서 첫 방문 혜택 50원을 자동 요청한다. 온보딩 완료나 경기 예측을 추가 지급 조건으로 두지 않는다.
- 실제 프로모션 코드 `01M1XAXHHF8KX6JA7H86RXFGZH`, 계정당 1회, 종료일 2026-09-21 KST. 기존 방문자 중 미지급 사용자도 다음 방문에 최초 1회 수령할 수 있다.
- 토스 로그인에서 서버가 확인한 계정 식별자만 사용한다. 클라이언트는 지급 금액·코드·대상을 지정하지 못한다.
- HMAC 식별자와 서버 전용 지급 원장, 원자적 작업 잠금으로 재설치·재가입·동시 요청의 중복 지급을 막는다.
- 외부 지급 전에 키와 시도 시각을 영구 기록한다. 타임아웃·미확정 응답은 동일 키의 결과부터 조회한다. 미확정 키가 만료되어도 새 키로 재지급하지 않는다.
- ‘나’ 탭에서 지급 상태와 재확인을 제공하고, 지급 완료가 확인된 경우에만 성공 안내를 표시한다. 토스 포인트와 앱 내 XP를 구분한다.
- 프로모션 기록·보관 기간 안내 추가. 행사 종료 90일 후 정기 삭제 작업 포함.
- ‘나 → 리그별 적중률’은 `isMajorCompetition`의 8개 대회만 표시한다. 다른 대회의 경기·예측 데이터와 전체 적중률 계산은 유지한다.

## 검증

- `npm test`: **69개 통과**.
- `npx tsc -b`: 통과.
- Deno 서버 타입 검사: 통과 (`--node-modules-dir=none`).
- PGlite: 동시 요청 잠금, 만료 잠금 복구, 오래된 작업자의 저장 차단, 탈퇴 후 재가입, 테스트/운영 분리, 클라이언트 권한 차단, 고정 지급 금액 검사 통과.
- 본인 지정 계정(기존 자동 닉네임 `축잘알97b282`의 UUID 접두사로 동일 계정 식별)으로 TEST 코드 지급 성공. 테스트는 실지급 없음.
- 같은 계정으로 **운영 코드 실제 50원 지급 성공**, 2026-09-11 11:17:38 KST.
- 동일 요청을 동시에 두 번 반복해도 모두 `newlyPaid:false`, 추가 지급 없음.
- 변조한 금액 요청, 미인증 요청, 사용자 직접 원장 RPC 요청 차단 확인.
- 콘솔에서 실제 소진액 **50 / 100,000원** 확인.
- 임시 인증 세션은 검증 후 해당 세션만 로그아웃. 테스트 사용자 설정은 검증 후 제거한다.
- 320px 로그인 혜택 안내 및 내 기록의 메이저 8개 대회 표시를 브라우저에서 확인.
- 운영 번들의 프로모션 엔드포인트·DB 대상·메이저 ID 목록 확인. TEST 코드 및 개발 미리보기 엔트리 없음.
- 새 번들의 토스 실기기 화면 조작은 직접 검증하지 않았다. 본인에게 콘솔 테스트 링크를 발송했다.

## 운영 시 참고

- 지급 상태 `pending`에서 같은 키가 장시간 확인되지 않으면 원장과 토스 지급 내역을 확인한다. 새 키를 만들어 수동 재지급하지 않는다.
- 프로모션 예산은 기존 10만 원으로 유지했다. 예산 증액·새 캠페인 생성은 하지 않았다.
- 콘솔의 프로모션 종료일을 연장하거나 코드를 바꿀 경우 서버·화면의 기간 및 원장 제약·파기 기준도 함께 변경해야 한다.
- 프로모션 검증 스크립트: `scripts/verify-promotion-live.mjs` (`prepare`, `test`, `live`, `inspect`). `live`는 실제 금전 지급을 발생시킬 수 있으므로 승인된 계정·캠페인에서만 실행한다.

## 제출한 출시 노트

1. 첫 방문 혜택의 실제 지급 연동을 수정했어요. 토스 로그인 후 계정당 한 번 토스 포인트 50원을 자동으로 지급해요.
2. 통신 오류가 발생하면 기존 지급 결과를 확인하고, 재가입·재설치 및 중복 요청으로 포인트가 중복 지급되지 않도록 보완했어요.
3. 나 탭의 리그별 적중률에는 프리미어리그, 분데스리가, 리그앙, 라리가, 세리에 A, 챔피언스리그, 유로파리그, 컨퍼런스리그만 표시해요.
4. 첫 방문 혜택 안내, 지급 상태 확인, 개인정보 처리방침을 보완했어요.
검증: 69개 테스트, 서버 타입 검사 및 데이터베이스 권한·중복 지급 검사 통과. 운영 본인 계정으로 실제 50원 지급과 반복 요청 추가 지급 없음을 확인했어요.
