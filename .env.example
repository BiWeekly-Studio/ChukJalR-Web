# 비워두면 목업 데이터로 동작한다. 채우면 Supabase 를 바라본다.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 개발용 익명 로그인. 토스 앱 밖에서 테스트할 때만 켠다.
# 대시보드에서 Authentication → Anonymous sign-ins 도 켜야 한다.
VITE_DEV_ANON_AUTH=true

# Edge Function 전용 (클라이언트 번들에 절대 들어가면 안 된다)
# supabase secrets set API_FOOTBALL_KEY=...
